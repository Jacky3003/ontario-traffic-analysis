#include <iostream>
#include <rarray>

#include "mathfunctions.h"
#include "output.h"

using namespace std;

int main(int argc, char *argv[]){
    double test = asinx(0.5, 90.0);
    cout << "Starting project with some sin(x): " << test << endl;
}